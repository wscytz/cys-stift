# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
out/
dist/
build/
*.tsbuildinfo

# Tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Turbo / cache
.turbo/
.cache/

# Env / secrets
.env
.env.local
.env.*.local
*.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# Test / coverage
coverage/
*.log

# Media (user data, never in git — see §11 data dir)
**/media/

# Tauri signing
*.key
*.pem

# One-off audit scripts (underscore prefix = ad-hoc, not shipped tooling)
scripts/_*.cjs

# One-off product overview reports (not shipped, learning/recap only)
docs/audit/product-overview-*.md

# Undocumented UX-audit scratch screenshots (auto-generated, no doc reference)
docs/design/screenshots/phase-ux-mmxa/

# ── AI 开发过程文档(不上公开仓库) ─────────────────────────────────────────
# 这些是 AI 辅助开发的过程产物(plans / decisions / 审查 / 反馈 /
# superpowers 技能 / 归档 / 冒烟截图),对 GitHub 访客无价值且含大量内部讨论。
# 公开仓库保留:产品门面(README)、设计契约(specs / design)、用户文档(user)、
# 开发指南(development)、架构(architecture + adr)、状态(STATE / changelog)、
# 参考(reference)。
# 注:adr/ 是架构决策记录(非过程日志),spec 引用它 → 公开;要恢复其它目录公开,
# 在对应行前加 `!` 例外。
docs/plans/
docs/decisions/
docs/reviews/
docs/feedback/
docs/archive/
docs/superpowers/
docs/design/screenshots/
docs/screenshots/

