# E1: auto-generated platform icons from `cargo tauri icon`. These are build
# artifacts — tauri generates them every build from the source icon.png. We
# commit only the source (icon.png/icon.icns) + the 4 paths in bundle.icon.
Square*.png
64x64.png
StoreLogo.png
android/
ios/
icon.ico