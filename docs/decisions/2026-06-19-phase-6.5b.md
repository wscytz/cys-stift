# Phase 6.5b · Inbox 多媒介编辑

**日期**:2026-06-19
**状态**:✅ 完成
**tag**:`v0.8.2-phase-6.5b`
**执行模式**:主模型(Claude)按 plan 手动执行 + 自审(30 轮路线图第 4 轮)

---

## 一句话

Inbox 详情 Modal 编辑模式**完整暴露** links / codeSnippets / quotes 编辑器(原 Phase 3 MVP 只暴露 title + body,违反 spec §4.2)。抽 editors 到 `features/card/` 共享切片,CreateCardForm + CardDetail 双消费。**domain / db 零改动**,0 新依赖。

## 范围

按 [`docs/plans/2026-06-19-phase-6.5b-multi-media-edit.md`](../../plans/2026-06-19-phase-6.5b-multi-media-edit.md) 全部完成(Lean 范围)。

### ✅ 做了
- `apps/web/src/features/card/editors.tsx`(新):`ListEditor` / `CodeEditor` / `QuoteEditor` + `editorStyles` + 3 个 draft→payload 转换函数
- `apps/web/src/app/inbox/page.tsx`(改):详情 Modal `CardDetail` 编辑模式暴露 3 个 editor + `onSave` 扩 5 字段 patch + state 同步 useEffect + Phase 3 hint 移除
- `apps/web/src/app/inbox/create-card-form.tsx`(改):从本地编辑器实现改为 import 自 features/card/editors
- `scripts/p6.5b-shots.cjs`(新):7 项交互断言 + 6 截图
- `docs/design/screenshots/phase-6.5b/`(新):6 PNG + README
- **domain / db 零改动** + **0 新依赖**(`update can swap multi-media arrays` vitest 已覆盖)

### ❌ 没做(明确留后)
- Canvas `CardDetailModal` 多媒介编辑 → 后续 P6.5+ 统一(避免触碰 tagged Phase 4)
- Archive tile onClick 接通 → 后续 P6.5+
- Edit-mode 草稿 → 后续 P6.5+
- Edit 实时预览(Markdown)→ 留后
- Edit `<textarea>` 升级 monospace 文本编辑器 → 留后

## 核心承诺验证(spec §4.2 字段完整性)

| 断言 | 结果 |
|---|---|
| View 渲染原始 links/code/quotes | ✓ |
| Edit mode 暴露 3 个 editor(.le 块) | ✓ |
| Phase 3 hint 移除(.detail__hint 不存在) | ✓ |
| Title 改成 "Edited title" | ✓ |
| Link 替换为 "https://edited.example" | ✓ |
| Code 加到 2 个 | ✓ |
| Quote attribution 改成 "New attribution" | ✓ |
| 跨刷新保留 | ✓ |
| 零 page error | ✓ |

## 关键工程决策

1. **editors 抽到 `features/card/editors.tsx`**:CreateCardForm + CardDetail 双消费,避免重复。
2. **`editorStyles` 导出共享 CSS**:每个 consumer `<style>{editorStyles}</style>`,不堆 .le*。
3. **draft→payload 转换集中**:`draftLinksToPayload` / `draftCodesToPayload` / `draftQuotesToPayload` 双消费共用。
4. **`onSave` 扩 5 字段 patch**:title + body + links + codeSnippets + quotes。
5. **state 同步 useEffect deps 加 3 字段**:打开不同卡 / 外部 update 时 5 state 全重置。
6. **`CardService.update` 白名单已含 3 字段**(Phase 3 实现,无需扩 domain);`update can swap multi-media arrays` vitest 已覆盖。
7. **Phase 3 hint 移除**:"intentionally not exposed (Phase 3 MVP)" 删了。
8. **Canvas CardDetailModal 不动** + **Archive tile 不接通**(Lean 排除)。
9. **0 新依赖** + **domain / db 零改动**。

## 验收

- ✅ `pnpm --filter domain test` 10 全绿(domain 零改动,已有 update multi-media test 覆盖)
- ✅ `pnpm --filter db test` 7 全绿(db 零改动)
- ✅ `pnpm --filter web build` exit 0,12 静态页
- ✅ spec §4.2 字段完整性:links / codeSnippets / quotes 编辑器全暴露
- ✅ 6 色 token / 字体 / 8px 网格 不破
- ✅ `features/card/` + `app/inbox/` hex grep 零命中
- ✅ puppeteer 7/7 交互断言
- ✅ 6 截图 + 视觉对照笔记

详见 [`docs/changelog.md`](../../changelog.md) phase 6.5b 段 + [`docs/design/screenshots/phase-6.5b/README.md`](../../design/screenshots/phase-6.5b/README.md)。