# Phase 6 · 捕获入口（全局快捷键 + Mini Input）

**日期**：2026-06-19
**状态**：✅ 完成
**tag**：`v0.7.0-phase-6`
**执行模式**：主模型（Claude）按 plan 手动执行 + 自审（Ralph 已停用，见根 `CLAUDE.md` "Ralph 状态"）

---

## 一句话

`Cmd/Ctrl+Shift+Space` 任意位置触发 Mini Input → 输入即保存到 Inbox。3 秒内完成"灵感 → 落库"。`CaptureSink` 接口 web 端落地（`WebCaptureSink` 走 `service.fromCapture`）。**0 新依赖 / domain+db 零改动**。

## 范围

按 [`docs/plans/2026-06-19-phase-6-capture-entry.md`](../../plans/2026-06-19-phase-6-capture-entry.md) 全部完成（Lean 范围）。

### ✅ 做了
- `apps/web/src/features/capture/capture-sink.ts`（新）：`interface CaptureSink` + `class WebCaptureSink implements CaptureSink`（走 `service.fromCapture`）
- `apps/web/src/features/capture/mini-input.tsx`（新）：Mini Input 组件（spec §5.5 视觉：居中浮层 + 2px 红边 + 顶部 8px 红条 + z-index 200 + Escape/Enter/Cmd+Enter 键盘）
- `apps/web/src/features/capture/capture-host.tsx`（新）：挂载 `keydown` 监听 + `useDb()` 注入 service + 单实例 Mini Input
- `apps/web/src/app/layout.tsx`（改）：挂 `<CaptureHost />`（root layout，所有路由生效）
- `apps/web/src/app/page.tsx`（改）：首页新增 Capture 入口（红条 + ⌘ icon + 快捷键提示）
- `scripts/p6-shots.cjs`（新）：9 张截图 + 8 项交互断言
- `docs/design/screenshots/phase-6/`（新）：9 张 PNG + README 笔记
- **domain / db 零改动**（`service.fromCapture` 已有，Phase 2 实现 + 1 个 vitest 覆盖）

### ❌ 没做（明确留 Phase 6+ / 后续）
- Tauri 全局快捷键（`@tauri-apps/plugin-global-shortcut`）→ Phase 6+
- 菜单栏 / menubar capture → Phase 6+
- 编辑多媒介（详情 Modal 改 links/code/quotes）→ Phase 6+
- inbox → canvas send 动作 → Phase 6+
- 图片上传 / MediaAsset 落盘 → Phase 6+
- 链接 OG 抓取 → 留后
- 草稿自动保存 → 后续
- 快捷键自定义 → 后续
- 多 CaptureSink 实现（spec §7 列 5 个待实现）→ 本阶段仅 web 1 个
- 手动 capture（inbox CreateCardForm）改用 WebCaptureSink → 留 Phase 6+（避免触碰 tagged Phase 3）

## 核心承诺验证（spec §8 Phase 6 段）

| 断言 | 结果 |
|---|---|
| 快捷键 `Cmd/Ctrl+Shift+Space` 在 `/` 触发 | ✓ |
| Enter 标题 focus 展开 body | ✓ |
| Cmd+Enter 保存 + 关闭 | ✓ |
| `card.source.kind === 'shortcut'`, `shortcutId === 'cmd-shift-space'`, `deviceId === 'web'` | ✓ |
| 跨刷新保留 | ✓ |
| 快捷键在 `/canvas` 也工作 | ✓ |
| 焦点在 input 内**不**触发 | ✓ |
| 零 page error | ✓ |

## 关键工程决策

1. **`CaptureSink` 接口放 `features/capture/` 而非 domain**：依赖倒置，web-local 接口 + 实现，domain 不感知。`CardService.fromCapture` 作为底层统一入口。
2. **Mini Input 不复用 `<Modal>`**：Modal 1px 黑边 + z-index 100 vs Mini Input 2px 红边 + z-index 200 抢眼。自建 `.mi-*` CSS。
3. **`Input` 不 forwardRef，用 `autoFocus` 兜底**：Phase 1 Input 没 forwardRef；`MiniInput` 早返 `null` 后再渲染那一拍浏览器 `autoFocus` 触发。
4. **首页 Capture 入口纯展示**（无 onClick）：避免 event bus 跨组件通信；按快捷键即可。
5. **Enter 展开 body 用 `placeholder` 字符串判别 active element**：Mini Input 内只有一个 Input；用 ref 更鲁棒但 Input 不支持；placeholder 匹配是合理 trade-off。
6. **puppeteer 用 `Control+Shift+Space`**：macOS Chrome headless 模式 `Meta+Shift+Space` 被 Spotlight 系统级拦截；浏览器内 `Control` 跨平台一致，**真实用户 `Cmd+Shift+Space` 浏览器内仍工作**（CaptureHost 接受 `metaKey || ctrlKey`）。
7. **0 新依赖**：沿用 react + domain `fromCapture`（已有）+ Phase 1 组件库。
8. **不重构 inbox CreateCardForm**：tagged Phase 3 代码，Lean 排除。

## 验收

- ✅ `pnpm --filter domain test` 10 全绿（domain 零改动）
- ✅ `pnpm --filter db test` 7 全绿（db 零改动）
- ✅ `pnpm --filter web build` exit 0，10 静态页（含 `/`）
- ✅ spec §8 Phase 6 三件 + §5.5 Mini Input 视觉 + §7 CaptureSink 接口
- ✅ 6 色 hex / 字体 / 8px 网格 在 Mini Input 仍对
- ✅ `features/capture/` + `app/page.tsx` hex grep 零命中
- ✅ puppeteer 8/8 交互断言
- ✅ 9 截图 + 视觉对照笔记

详见 [`docs/changelog.md`](../../changelog.md) phase 6 段 + [`docs/design/screenshots/phase-6/README.md`](../../design/screenshots/phase-6/README.md)。