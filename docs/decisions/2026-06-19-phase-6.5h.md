# Phase 6.5h · 快捷键自定义

**日期**:2026-06-19
**状态**:✅ 完成
**tag**:`v0.8.8-phase-6.5h`
**执行模式**:主模型(Claude)按 plan 手动执行 + 自审(30 轮路线图第 10 轮)

---

## 一句话

`/settings` 路由 + web-local settings store + CaptureHost 读 settings。默认 ⌘+⇧+Space,可改成 ⌘+⇧+C 等。**0 新依赖**,domain/db 零改动。

## 范围

按 [`docs/plans/2026-06-19-phase-6.5h-keymap-custom.md`](../../plans/2026-06-19-phase-6.5h-keymap-custom.md) 全部完成(Lean 范围)。

### ✅ 做了
- `apps/web/src/lib/settings-store.ts`(新):`Settings { captureShortcut }` + `settingsStore.get/update/updateCaptureShortcut` + `useSettings` hook
- `apps/web/src/app/settings/page.tsx`(新):`/settings` 路由 + modifier/shift/key 下拉 + 实时显示
- `apps/web/src/features/capture/capture-host.tsx`(改):keydown 读 `settings.captureShortcut`(deps 含 sc.code)
- `apps/web/src/components/app-menu.tsx`(改):加 Settings 入口
- `scripts/p6.5h-shots.cjs`(新):5 项断言 + 3 截图
- `docs/design/screenshots/phase-6.5h/`(新):3 PNG + README
- **domain / db 零改动** + **0 新依赖**

### ❌ 没做(明确留后)
- 冲突检测 → 留后
- 录制式捕获 → 留后
- canvas 快捷键自定义 → 留后
- Tauri 端读 settings → Phase 8

## 核心承诺验证(spec §5.5 "可在设置改")

| 断言 | 结果 |
|---|---|
| /settings 默认 `⌘+⇧+Space` | ✓ |
| 改成 `⌘+⇧+C` | ✓ |
| localStorage 持久化(`captureShortcut.code === 'KeyC'`) | ✓ |
| 按新组合打开 Mini Input | ✓ |
| 零 page error | ✓ |

## 关键工程决策

1. **web-local settings store**(同 draft/canvas-view 模式)。
2. **CaptureHost 接受 meta OR ctrl**(跨平台)。
3. **`useSettings` + keydown deps 含 sc.code**:re-bind 无需刷新。
4. **下拉式 UI**(非录制式):MVP。
5. **0 新依赖** + **domain / db 零改动**。

## 验收

- ✅ `pnpm --filter domain test` 11 全绿
- ✅ `pnpm --filter db test` 7 全绿
- ✅ `pnpm --filter web build` exit 0,13 静态页(新增 /settings)
- ✅ spec §5.5 "可在设置改"
- ✅ 6 色 token / 字体 / 8px 网格 不破
- ✅ `lib/settings-store.ts` + `app/settings/` + `components/` hex grep 零命中
- ✅ puppeteer 5/5 交互断言
- ✅ 3 截图 + 视觉对照笔记

详见 [`docs/changelog.md`](../../changelog.md) phase 6.5h 段 + [`docs/design/screenshots/phase-6.5h/README.md`](../../design/screenshots/phase-6.5h/README.md)。