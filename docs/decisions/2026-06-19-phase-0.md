# 2026-06-19 · Phase 0 脚手架完成

> T1–T7 全部落地。

## 交付

- pnpm monorepo：`apps/{web,desktop}` + `packages/{ui,db,domain,config}`
- `apps/web`：Next.js 15 静态导出 + React 19 + TS strict + 包豪斯 token 占位首屏
- `apps/desktop`：Tauri v2 壳（Rust 工具链就绪后即可 `pnpm tauri dev`）
- 根配置：`.nvmrc` (22) / `.gitattributes` (LF) / `.editorconfig` / `.gitignore` / `.prettierrc` / `tsconfig.base.json`
- 文档：README + architecture/overview + 6 个 ADR + memory 系统 + design tokens + setup + changelog

## 验证

- `pnpm install` ✅
- `pnpm --filter web build` → 静态产物生成（Phase 0 当下阶段）
- `pnpm tauri dev` → 待 Rust 装好后验证
- Windows 端 `pnpm install` + `pnpm build` → 待跨平台复验

## 已知限制

- Phase 0 零业务逻辑；首屏是占位页
- Tauri 编译首次较慢（Rust 依赖下载 + 编译），属正常
