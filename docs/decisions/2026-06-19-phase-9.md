# Phase 9 · JSON 导出 + 用户文档

**日期**:2026-06-19
**状态**:✅ 完成
**tag**:`v0.9.0-phase-9`
**执行模式**:主模型(Claude)按 plan 手动执行 + 自审(30 轮路线图第 11 轮)

---

## 一句话

`/settings` 加 Export JSON 按钮 → 下载开放格式 `.json`(`{version, exportedAt, app, cards, mediaAssets, drafts, settings}`)。用户文档 `docs/user/README.md`。**兑现 spec §1.2 信念4 "数据可迁移,不做锁定"。0 新依赖。**

## 范围

按 [`docs/plans/2026-06-19-phase-9-export.md`](../../plans/2026-06-19-phase-9-export.md) 全部完成(Lean 范围)。

### ✅ 做了
- `apps/web/src/lib/export-service.ts`(新):`EXPORT_FORMAT_VERSION` + `ExportPayload` + `buildExportPayload` + `downloadExport`
- `apps/web/src/app/settings/page.tsx`(改):Data section + Export JSON 按钮
- `docs/user/README.md`(新):用户指南
- `scripts/p9-shots.cjs`(新):8 项断言 + 2 截图
- `docs/design/screenshots/phase-9/`(新):2 PNG + README
- **domain / db 零改动** + **0 新依赖**

### ❌ 没做(明确留后)
- 反向 import → 留后
- 录屏 → 留后
- `/changelog` 路由 → 留后

## 核心承诺验证(spec §1.2 信念4)

| 断言 | 结果 |
|---|---|
| 下载 1 个 JSON | ✓ |
| `version === 1` | ✓ |
| `cards.length === 2` | ✓ |
| `mediaAssets` 1 key | ✓ |
| `settings.captureShortcut.code === 'KeyC'` | ✓ |
| `exportedAt` ISO string | ✓ |
| 零 page error | ✓ |

## 关键工程决策

1. **开放格式 JSON,版本化**(`version: 1`):任何工具可读。
2. **导出范围**:cards + mediaAssets + drafts + settings。
3. **浏览器原生下载**:0 新依赖。
4. **纯函数 + 副作用分离**。
5. **用户文档**:核心流程 + 数据隐私 + 快捷键。
6. **0 新依赖** + **domain/db 零改动**。

## 验收

- ✅ `pnpm --filter domain test` 11 全绿
- ✅ `pnpm --filter db test` 7 全绿
- ✅ `pnpm --filter web build` exit 0,13 静态页
- ✅ spec §1.2 信念4 "数据可迁移"
- ✅ 6 色 token / 字体 / 8px 网格 不破
- ✅ `lib/export-service.ts` + `app/settings/` hex grep 零命中
- ✅ puppeteer 8/8 交互断言
- ✅ 2 截图 + 视觉对照笔记 + 用户文档

详见 [`docs/changelog.md`](../../changelog.md) phase 9 段 + [`docs/design/screenshots/phase-9/README.md`](../../design/screenshots/phase-9/README.md) + [`docs/user/README.md`](../../user/README.md)。