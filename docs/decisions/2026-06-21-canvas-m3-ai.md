# 2026-06-21 · v0.29.0-canvas-m3-ai

> 来源: 用户对话(2026-06-21:加 AI 元素 — 先做 AI 访问设置 + 定制化操作 + 参考 AFFiNE 0.26.3 架构)。

## 设计

M3 给 cy's Stift 加一个**完全可选、可关闭、密钥不外泄**的 AI 层。**核心约束**:本地优先 — 没配 AI 时应用一切照旧(零按钮、零状态、零 main chunk 增量;3 个 provider 走 dynamic-import-style 注册,factory 注册 12 行,providers/index.ts 的 registerDefaultProviders 在第一次用户操作时调一次)。

### 关键决策

1. **3 provider:OpenAI / Anthropic / Ollama** —— 不做 Azure / Gemini / 本地 GGUF 直接加载(用户明确选 3 个 + Ollama 已覆盖本地场景)。每个 provider 用原生 HTTP + SSE/NDJSON,**不开 SDK**(openai/anthropic SDK 是 Node 优先,bundle +100KB+)。
2. **API key 明文 localStorage + UI 警告 banner** —— 不做 AES-GCM(本地优先,YAGNI;用户接受风险)。Warning 文案显式说明「仅本机 / 不上传 / 公共设备勿用」。**M4 可升级 OS keychain**(browser 端 `window.crypto.subtle` 可用,但需用户主动解锁)。
3. **Maker-based factory**(存 `Map<ProviderId, (cfg) => AIProvider>`)—— OpenAI/Anthropic 的 apiKey 必须闭包进 instance,不能共享。Factory = 「maker 字典」,每次 `create(cfg)` 现场 new,防止 apiKey 泄漏到跨 config 实例。
4. **AI 按钮在 `ai === null || !enabled` 时不渲染**(不是 disabled)—— 用户没配 AI 时,UI 完全干净,符合「本地优先不打扰」原则。Card-detail `cd__actions` 区只在 `useAIEnabled()` 返回 true 时挂 3 个 ✨ 按钮;canvas-toolbar 同样只在 `useAIEnabled() && selectedCardsCount >= 2` 时挂 ✨ auto-relate。
5. **3 个卡片 action + 1 个画布 action**(summarize / improveWriting / translate / autoRelate)—— 不抄 AFFiNE 的 30+(他们有云端协作语境)。`translate` 子菜单 zh↔en 二选一,`improveWriting` UI label 是 `Rewrite`(labelKey 'card.rewrite')。
6. **画布 auto-relate 复用 M2.1 `createArrowFromHandle`** —— 不重写箭头创建路径。`inferRelationTypeFromContext` 复用 M2.3 关键词匹配(命中就建箭头,没命中跳过,避免噪声)。
7. **三协议 SSE 解析策略** —— OpenAI 手写 SSE 累加(`data:` 简单);Anthropic 用 `eventsource-parser`(v1.1.2 API 是 `createParser((ev) => {...})` 直接接函数,非 v2 的 `{onEvent: ...}`);Ollama 走 NDJSON(不是 SSE,`done: true` 终止)。
8. **AI popover 内嵌浮层**而不是 Modal —— 流式输出时 Modal 体验差;inline 浮在按钮旁 + Replace / Append as new / Cancel 三选项。AbortController 在 unmount 触发,AbortError 静默吞掉。
9. **`getCurrentAI()` module-level singleton** —— `<AIProviderSync />` 在 layout 订阅 settings-store 推到 module-level;popover 异步流式渲染中 hook 重渲染易出 stale closure,同步读最直接。Card-detail 的 `useAIEnabled()` 走 hook(用于按钮条件渲染),popover 走 `getCurrentAI()`(用于读 cfg 发起 fetch)。
10. **vitest 引入** —— web 包从 0 测试到 12 测试(纯函数 / provider factory / safe-href AI 校验);jsdom env。**只测纯逻辑,不测 React 组件**(避免 testing-library 增量)。

### 不做(显式 defer)

- AI 关系类型自定义 prompt(M3 用硬编码 4 类型,用户已在 M2.3 接受关键词匹配)
- AI 自动生成全新卡片(本 plan 是「操作现有卡」,不是「造卡」)
- AI 嵌入(embedding) / 向量搜索(本地优先边界 + 当前数据量级不需要)
- 流式输出 cancel 按钮(AbortSignal 已接,AbortError 静默;M3.1 可补)
- AES-GCM 加密 API key(YAGNI;M4 可加,基于 OS keychain)
- Provider 配额监控(各家 API 自带 dashboard,UI 重复)
- 多 key 轮换 / 多模型 ensemble(单 key 足够)
- 多模态(image input 给 GPT-4V / Claude vision)—— M2.2 已经支持图片 capture,M3 不接 vision
- AI 创建箭头的概率/置信度阈值(目前「命中关键词就建」,没命中跳过)
- SSE retry / reconnect(用户主动 retry)
- Provider 配额耗尽 graceful degradation(M4)
- **Canvas modal (M2.5 export 也没接) 接入 AI 按钮** —— 计划原话「card-detail-modal 接同样 props」,但 canvas modal 没有 actions prop 概念、且 export 按钮在 canvas 也没接。强行扩会改 canvas modal 内部 button list,超出 M3 范围。**M3.1 / M4 可补**。

## 验收

- domain 26/26 + db 7/7 + web build exit 0
- web test 12/12(新增 provider factory + safe-href AI)
- e2e m3-shots 7/7 passed
- 新 dep: eventsource-parser (1 runtime) + vitest + @vitest/ui + jsdom (3 dev)

## 交付文件清单

| 文件 | 责任 |
|---|---|
| `apps/web/src/features/ai/types.ts` | AIProvider / AIRequest / AIResponse / AIConfig / ProviderId / AIProviderMaker 类型 |
| `apps/web/src/features/ai/provider-factory.ts` | maker-based factory(register/unregister/list/create) |
| `apps/web/src/features/ai/providers/{openai,anthropic,ollama}.ts` | 3 个 provider 实现 |
| `apps/web/src/features/ai/providers/index.ts` | 默认注册 + `getDefaultProviderDefaults` |
| `apps/web/src/features/ai/stream-text.ts` | 通用编排 |
| `apps/web/src/features/ai/test-connection.ts` | 连接测试 |
| `apps/web/src/features/ai/__tests__/provider-factory.test.ts` | vitest 单测 |
| `apps/web/src/features/ai/prompts.ts` | 3 个 prompt 模板 |
| `apps/web/src/features/ai/ai-actions.ts` | `runAIAction` 编排 |
| `apps/web/src/features/ai/ai-popover.tsx` | 流式输出 + 三选项浮层(内联 styles) |
| `apps/web/src/features/ai/ai-settings-provider.tsx` | module-level AI config 同步 + `useAIEnabled` |
| `apps/web/src/features/settings/ai-settings-panel.tsx` | /settings AI 面板 |
| `apps/web/src/features/canvas/auto-relate.ts` | 画布 AI 自动关系 |
| `apps/web/src/features/canvas/card-service-access.ts` | `__cardService` 全局读取 helper |
| `apps/web/src/features/card/card-detail.tsx` | + 3 AI actions + 按钮 + AIPopover + onAIAppendNew prop |
| `apps/web/src/app/inbox/page.tsx` | actions 数组含 3 AI actions + onAIAppendNew 走 captureSinkRegistry |
| `apps/web/src/app/archive/page.tsx` | actions 数组含 3 AI actions + onAIAppendNew 走 captureSinkRegistry |
| `apps/web/src/app/layout.tsx` | 挂 `<AIProviderSync />` |
| `apps/web/src/lib/settings-store.ts` | + ai 字段 + `updateAISettings()` + `isValidAIConfig` |
| `apps/web/src/lib/safe-href.ts` | + 3 AI 校验 |
| `apps/web/src/lib/i18n/messages.ts` | + 21 个新 key(settings.ai* / card.summarize/rewrite/translate / ai.* / canvas.autoRelate*) |
| `apps/web/src/app/settings/page.tsx` | + `<AISettingsPanel />` + 5 个新 CSS 类 |
| `apps/web/src/features/canvas/canvas-toolbar.tsx` | + ✨ auto-relate 按钮(多选时) |
| `apps/web/vitest.config.ts` | jsdom + @ alias |
| `apps/web/package.json` | + eventsource-parser + vitest + @vitest/ui + jsdom |
| `scripts/m3-shots.cjs` | e2e 7 段 |
| `docs/design/screenshots/m3/` | 2 张截图 |

---

## 后续路线(2026-06-21 对话确认)

### M3 收尾候选(小任务,可任意顺序)

- **canvas modal AI 接入**:`@/features/canvas/card-detail-modal.tsx` 没有 actions prop 概念,export 也没接;M3.1 可补
- **OS keychain 加密 API key**:`window.crypto.subtle` 可用,需用户主动解锁;非阻塞
- **流式 cancel UI**:AbortSignal 已接,UI 按钮可补
- **Agent loop 探索**:多轮 tool calls,token 用量爆炸,**M4+**

### M3.1 文字 DSL 排版(下一活跃 phase)

**用户决策(2026-06-21 对话)**:
- ❌ **不做多模态** — 外围支持不成熟,本地优先边界,vision 模型贵
- ✅ **纯文本 + DSL 语法** 路径:AI 读 cards 文本列表 + 简短 prompt,输出 markdown-link 风格的 DSL 描述布局

**核心难点:手绘内容**:
- 用户的 free-draw 笔触 / 自由便签 / 手画关系箭头 = 视觉装饰,**不进 AI 视野**
- AI 排版只动结构化元素:cards 位置 / 颜色(group)/ 关系箭头
- 用户通过 card body 内的注释(如 `// 区域:工作`)驱动 AI 语义理解 — 注释 = 标签
- 不做 OCR 也不做 vision:手绘闭合曲线**不被 AI 解析**

**M3.1 计划要点**(未开 plan):
- DSL 语法:借鉴 markdown 链接(`[A] --rel--> [B]`) + 标签(`@cluster(健康) @pos(300,400) @color(red)`)
- AI 输入:JSON `[ {id, title, body, tag?} ]` + prompt("按主题 cluster 重排")
- AI 输出:DSL 文本流(OpenAI `response_format: { type: 'json_object' }` 强制 JSON,内部是 DSL 字符串)
- 客户端:DSL 解析器(~ 100 行,手写 PEG)+ `applyLayout(editor, parsed)` 调 updateShape × N
- UI:canvas toolbar 加 "📐 AI 排版" 按钮,弹 prompt 输入(M3 已知 pattern)
- e2e:reverse assert(无 AI 时按钮不渲染)
- 风险:DSL 解析需容错(AI 输出可能格式不完全符合,后处理兜底)

### 其他活跃候选

- **标签系统**:`Card.tag: string[]`,支撑 M3.1 cluster + 染色。复杂度中,先做可让 M3.1 更纯净
- **Tauri 打包**:本地未签名可直接出,签名公证需 Apple 证书
- **OPFS**:5MB localStorage 限制解除
- **录屏**:canvas 操作录制成 .webm(MediaRecorder)
- **UX 打磨**:inbox 批量操作 / minimap / 暗色模式 polish / Card markdown 双向编辑
