# 2026-06-19 · Phase 2 数据层完成

> 持久化承诺兑现：UI 写卡 → 跨刷新 → 数据仍在。

## 交付

- `packages/domain` 升级：types (Branded IDs + 所有 spec §4) + codec + Card/Canvas/Workspace services + 6 个 vitest 通过
- `packages/db` 升级：Drizzle schema (4 表) + codec + SqliteCard/Canvas/WorkspaceRepository + 7 个集成测试通过
- `apps/web/src/lib/db-client.ts` 客户端单例：useSyncExternalStore + in-memory + localStorage
- `apps/web/src/app/dev/db/page.tsx` 烟测页（counts / 表单 / inbox / archived / reset）
- `scripts/p2-shots.cjs` puppeteer 持久化证据脚本
- 4 张截图 + README 笔记：`docs/design/screenshots/phase-2/`

## 关键架构决策

1. **Repository 抽象成功演练**：spec §3.4 第一条"Repository 接口在 domain，存储适配器按运行时注入"已实现
   - Node 路径（db 包）：better-sqlite3 + drizzle
   - 浏览器路径（db-client）：in-memory + localStorage
   - **未来 Phase 2.5**：in-memory 替换为 wa-sqlite，business code 零改动
2. **better-sqlite3 而非 wa-sqlite**：wa-sqlite 在 Next 客户端需要 WASM + OPFS 复杂 plumbing，Phase 2 优先跑通 schema + tests
3. **hydration 修复**：`useSyncExternalStore` 的 snapshot object 引用稳定性

## 验证

- ✅ domain 6/6 tests pass
- ✅ db 7/7 tests pass
- ✅ web build 静态导出，104 kB First Load JS
- ✅ puppeteer 持久化断言：session A 写 3 张卡 → session B 3 张卡完整
- ✅ 视觉：6 色 / 字体 / 网格 / 组件仍对

## 已知 / 后续

- Phase 2.5：浏览器侧 in-memory + localStorage → wa-sqlite + OPFS
- Phase 6/8：Tauri 桌面端 fs 落盘验证
- Phase 3：MediaAsset 真实文件存盘
