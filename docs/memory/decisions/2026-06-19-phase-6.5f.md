# Phase 6.5f · 图片上传(占位)

**日期**:2026-06-19
**状态**:✅ 完成
**tag**:`v0.8.6-phase-6.5f`
**执行模式**:主模型(Claude)按 plan 手动执行 + 自审(30 轮路线图第 8 轮)

---

## 一句话

图片上传最小 MVP — `<input type=file>` + base64 inline 存 web-local localStorage + 详情 Modal view/edit 显示图片。spec §4.5 Web=OPFS 留 Phase 2.5;**当前为占位,大图警告**。**0 新依赖**。

## 范围

按 [`docs/superpowers/plans/2026-06-19-phase-6.5f-media-upload.md`](../../superpowers/plans/2026-06-19-phase-6.5f-media-upload.md) 全部完成(Lean 范围)。

### ✅ 做了
- `apps/web/src/lib/media-store.ts`(新):`mediaStore.attach / getAsset / remove` + base64 data URL + 软 500KB 警告
- `packages/domain/src/services/card-service.ts`(改):`UpdateCardPatch.media` 字段白名单 + `update` 函数体接受 media
- `packages/domain/src/__tests__/card-service.test.ts`(改):新加 `update can swap media array` 测试
- `apps/web/src/app/inbox/page.tsx`(改):CardDetail view 渲染 `<img>`;edit 加 file input + 缩略图列表 + × 删除
- `scripts/p6.5f-shots.cjs`(新):4 项交互断言 + 3 截图
- `docs/design/screenshots/phase-6.5f/`(新):3 PNG + README
- **0 新依赖** + **domain 1 个 vitest 新增** / db 零改动

### ❌ 没做(明确留后)
- OPFS 真实落盘 → Phase 2.5(独立 phase)
- Tauri fs 落盘 → Phase 8
- 图片编辑(裁剪/旋转)→ 留后
- 拖放上传 → 留后
- OG 图片抓取 → 留后

## 核心承诺验证(spec §4.5 MediaAsset 最小 MVP)

| 断言 | 结果 |
|---|---|
| 上传 1 张图 → save → `card.media.length === 1` | ✓ |
| `cys-stift.media.v1` 1 asset | ✓ |
| 详情 Modal 渲染 1 个 `<img>` | ✓ |
| 跨刷新保留 | ✓ |
| 零 page error | ✓ |

## 关键工程决策

1. **base64 inline localStorage 占位**:Phase 2.5/8 替换时,公共 API 不变。
2. **domain 扩 `UpdateCardPatch.media`**:补白名单,新加 1 vitest。
3. **软限制 500KB**:console.warn。
4. **0 新依赖**:FileReader / data URL 原生。

## 验收

- ✅ `pnpm --filter domain test` 11 全绿
- ✅ `pnpm --filter db test` 7 全绿
- ✅ `pnpm --filter web build` exit 0,12 静态页
- ✅ spec §4.5 MediaAsset 最小 MVP
- ✅ 6 色 token / 字体 / 8px 网格 不破
- ✅ `lib/media-store.ts` + `app/inbox/` hex grep 零命中
- ✅ puppeteer 4/4 交互断言
- ✅ 3 截图 + 视觉对照笔记

详见 [`docs/development/changelog.md`](../../development/changelog.md) phase 6.5f 段 + [`docs/design/screenshots/phase-6.5f/README.md`](../../design/screenshots/phase-6.5f/README.md)。